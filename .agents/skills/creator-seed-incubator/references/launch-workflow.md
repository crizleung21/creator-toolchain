# Launch Workflow

`creator-seed:launch` means graduate plus immediate handoff to `creator-paul-loop`.

## Rules

- Do not re-ask questions already answered in `PLANNING.md`.
- If required fields conflict or are missing, return to planning.
- `HANDOFF.md` must include target skill `creator-paul-loop`.
