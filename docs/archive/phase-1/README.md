# Phase 1 Archive

These files record the initial MVP skill-loop acceptance surface. They are retained for historical regression context and are not the current release gate.

Current capability and contract tests live under `docs/qa/`. The active SEED fixture lives at `docs/fixtures/seed/character-image-slide-project.md`.
